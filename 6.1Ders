#include <iostream>

using namespace std;//her türlü std komutlarını kısaltan satır

int main() {
    cout << "Deneme";
    int a;
    cin >> a;
        for (int i = 1; i <= a; ++i) { // for döngüsü (değişken tanımlama;karar;iş)
        cout << i << " ";//i değişkeni yazdırma
    }
    return 0;
}
